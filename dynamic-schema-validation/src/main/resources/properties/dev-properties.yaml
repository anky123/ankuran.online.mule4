#HTTP API Properties
http:
  private:
    port: "8091"
    host: "0.0.0.0"
    path: "/api/*"
#reconenct frequency
    reconnect:
      frequency: "10000"
      attempts: "2" 

#Country list that will support by the API
supported_countries : ['IN','US']